<div class="card goals-card">
    <div class="card-header">
      <h2><i class="bi bi-flag"></i> Goals</h2>
      <div class="card-controls">
        <button class="control-btn switch-btn">Switch</button>
        <button class="control-btn expand-btn">Expand</button>
      </div>
    </div>
    <div class="goals-content">
        <% if (goals.length === 0) { %>
            <p class="text-muted">No goals set yet.</p>
        <% } else { %>
            <ul class="goal-list">
                <% goals.forEach(goal => { %>
                    <li class="goal-item border-<%= goal.planType %>" data-plan-type="<%= goal.planType %>">
                        <div class="goal-header">
                            <span class="goal-title"><%= goal.title %></span>
                            <small>(<%= goal.completedTasks %> / <%= goal.totalTasks %> Completed)</small>
                        </div>
                        <div class="goal-progress">
                            <div class="progress-bar" style="width: <%= goal.progress %>%;"></div>
                        </div>
                    </li>
                <% }) %>
            </ul>
        <% } %>
    </div>
</div>
